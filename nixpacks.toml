[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build", "npm prune --omit=dev"]

[start]
cmd = "node dist/index.js"

[healthcheck]
path = "/ok"

[variables]
NODE_ENV = "production"
